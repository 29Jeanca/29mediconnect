import { BrowserRouter as Router, Route,Routes } from "react-router-dom";

const Routing = () =>{
    return(
        <Router>
            <Routes>
                <Route/>
            </Routes>
        </Router>
    )
}
export default Routing;